{
  "name": "lambda",
  "version": "1.0.0",
  "description": "test project for lambda",
  "main": "index.js",
  "scripts": {
    "run": "build/cli.js",
    "test": "LOG_LEVEL=SILENT jest --coverage --watchAll",
    "test:ci": "jest --coverage --ci",
    "build": "npm run clean && tsc && npm run install:prod",
    "install:prod": "npm install --production --prefix build && npm install --production --prefix ../backend && cp -R ../backend/build/* build/node_modules/performance-dashboard-backend",
    "clean": "rm -rf build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.84",
    "@types/aws-sdk": "^2.7.0",
    "@types/jest": "^27.0.2",
    "@types/uuid": "^8.3.1",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "ts-loader": "^9.2.6",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "aws-sdk": "^2.984.0",
    "envalid": "^7.2.2",
    "performance-dashboard-backend": "file:../backend",
    "uuid": "^8.3.2"
  }
}
