// @ts-nocheck
import { Configuration } from "../../common";
import { TopicAreaBuilder } from "../../builders/topicarea-builder";
import { TopicArea } from "performance-dashboard-backend/src/lib/models/topicarea";

export async function buildTopicAreas(config: Configuration) {
  const topicAreas: { [name: string]: TopicArea } = {};

  // <GENERATED_CODE>

  return topicAreas;
}
