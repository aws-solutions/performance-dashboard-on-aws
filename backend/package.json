{
  "name": "badger-backend",
  "version": "0.1.0",
  "description": "Badger Backend",
  "scripts": {
    "test": "jest",
    "build": "npm run clean && tsc && npm run install:prod",
    "copy:package": "cp package.json build/",
    "install:prod": "npm run copy:package && npm install --production --prefix build",
    "local": "ts-node src/local/server.ts",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.57",
    "@types/aws-serverless-express": "^3.3.3",
    "@types/express": "^4.17.6",
    "@types/jest": "^26.0.3",
    "@types/node": "^14.0.14",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "express": "^4.17.1",
    "aws-serverless-express": "^3.3.8"
  }
}
